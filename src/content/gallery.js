import { siteConfig } from './site'

export const galleryContent = {
  sectionTitle: siteConfig.gallery.sectionTitle,
  sectionText: siteConfig.gallery.sectionText,
  behavior: siteConfig.gallery.behavior,
  images: siteConfig.gallery.images,
}
