import { siteConfig } from './site'

export const servicesContent = {
  sectionTitle: siteConfig.services.sectionTitle,
  sectionText: siteConfig.services.sectionText,
  items: siteConfig.services.items,
  presentation: siteConfig.services.presentation,
}
